---
import PagesLayout from "@/layouts/PagesLayout.astro";
import PlanCard from "@/components/shared/PlanCard.astro";
import CardsContainer from "@/components/shared/CardsContainer.astro";
import SectionHeading from "@/components/shared/SectionHeading.astro";
import config from "@/data/config";
const classes = [
	"Yellow",
	"Green",
	"Purple",
	"LightBlue",
	"DarkGreen",
	"Orange",
	"LightOrange",
];
---

<PagesLayout
	title="Planes"
	description="Tenemos un plan para cada tipo de negocio y emprendimiento. Te ayudamos a elegir."
	image="/screenshot.png"
/>

<div class="Container">
	<CardsContainer>
		{
			config.plans.map((plan, index) => (
				<PlanCard
					price={plan.price}
					title={plan.name}
					description={plan.description}
					classes={classes[index % classes.length]}
					tag={plan.tag}
					features={plan.features}>
					{/* <a href={`/plans/${plan.slug}`} class="Button Button-sm Orange">
							Lo quiero!
						</a> */}
					<a href="/contact" class="Button Button-sm Orange">
						Lo quiero!
					</a>
				</PlanCard>
			))
		}
	</CardsContainer>
</div>
