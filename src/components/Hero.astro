---
import Button from "./shared/Button.astro";
---

<section class="Hero">
	<div class="Container">
		<h1 class="Hero-title shadow-titles">
			Páginas Web Económicas para PYMEs y emprendimientos
		</h1>
		<p class="Hero-description">
			Somos una familia dedicada al Marketing Digital con experiencia en
			servicios para grandes empresas, y dedicados a apoyar a las pequeñas y
			medianas empresas con páginas Web económicas de alta calidad.
		</p>
		<div class="Hero-actions">
			<Button
				text="Ver todos los planes"
				url="/plans"
				classes="Button Button-md Green"
			/>
			<Button
				text="Contáctanos"
				url="/contact"
				classes="Button Button-md Orange"
			/>
		</div>
	</div>
</section>

<style lang="scss">
	.Hero {
		background-image: linear-gradient(
				rgba(6, 8, 85, 0.726),
				rgba(0, 0, 0, 0.719)
			),
			url(/src/assets/site-images/FaroLaSerena-mobile.webp);
		background-repeat: no-repeat;
		background-size: cover;
		background-attachment: local;
		height: 100vh;
		display: flex;
		flex-flow: column nowrap;
		align-items: center;
		align-content: center;
		text-align: center;
		justify-content: center;
		gap: 1em;
		padding: 1em 0;
		color: var(--ui-white);

		&-actions {
			display: flex;
			gap: 1em;
			padding: 0.5em 0;

			@media (max-width: 480px) {
				flex-flow: column nowrap;
			}
		}
		&-title {
			text-transform: normal;
			color: var(--ui-white);
			font-size: 5rem;
			line-height: 1.5;
			margin: 0;
			font-weight: 800;
		}

		@media (max-width: 768px) {
			background-image: linear-gradient(
					rgba(4, 5, 48, 0.596),
					rgba(0, 0, 0, 0.719)
				),
				url(/src/assets/site-images/FaroLaSerena-mobile.webp);
			background-repeat: no-repeat;
			background-size: cover;
			background-attachment: local;
			height: unset;

			&-title {
				font-size: 2rem;
				line-height: 1.3;
			}
		}
	}
</style>
